apply from: rootProject.file('app-model-build.gradle')

android {
    defaultConfig {
        if (isRunModel.toBoolean()) {
            applicationId "com.model.login"
        }
    }
    //统一资源前缀，规范资源引用
    resourcePrefix "login_"
    namespace 'com.model.login'
}
dependencies {

}