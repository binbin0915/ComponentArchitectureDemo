<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.model.center">

    <application
        android:name="com.library.base.application.DebugApplication"
        android:allowBackup="false"
        android:icon="@mipmap/center_ic_launcher"
        android:label="@string/center_app_name"
        android:roundIcon="@mipmap/center_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CenterTheme.MyApplication">
        <activity android:name=".activity.CenterMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>