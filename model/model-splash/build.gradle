apply from: rootProject.file('app-model-build.gradle')

android {
    namespace 'com.model.splash'
    defaultConfig {
        if (isRunModel.toBoolean()) {
            applicationId "com.model.splash"
        }
    }
    //统一资源前缀，规范资源引用
    resourcePrefix "splash_"
}
dependencies {

}