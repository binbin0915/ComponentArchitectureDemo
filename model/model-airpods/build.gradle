apply from: rootProject.file('app-model-build.gradle')

android {
    namespace 'com.model.airpods'
    defaultConfig {
        if (isRunModel.toBoolean()) {
            applicationId "com.model.airpods"
        }
    }
    //统一资源前缀，规范资源引用
    resourcePrefix "airpods_"
}
dependencies {

}